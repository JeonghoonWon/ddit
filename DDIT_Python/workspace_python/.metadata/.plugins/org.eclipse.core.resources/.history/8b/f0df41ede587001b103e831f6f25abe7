# import pymysql
# import matplotlib.pyplot as plt
#
# mydb = pymysql.connect(
    # host = "localhost",
    # user = "root",
    # password = "python",
    # database = "python"
    # )
    #
# mycursor = mydb.cursor()
#
# sql = "select s_price, s_name, in_date from stock"
#
# mycursor.execute(sql)
#
# #result = mycursor.fetchall()
#
# #print(result)
#
# rows =mycursor.fetchall()
#
#
# for eachLine in rows:
    # x = eachLine[0]
    # y = eachLine[1]
    # z = eachLine[2]
    #
    # plt.hist(x, label='bins=10')
    #
# print(x,y,z) 
# #plt.hist(weight, bins=30, label='bins=LG')
# #plt.legend()
# plt.show()

import pymysql
import matplotlib.pyplot as plt

mydb = pymysql.connect(
    host = "localhost",
    user = "root",
    password = "python",
    database = "python"
    )

mycursor = mydb.cursor()    

sql = "select s_price, s_name, in_date from stock where s_name = 'LG'"

mycursor.execute(sql)

rows = mycursor.fetchall()


for eachLine in rows:
    x = eachLine[0]
    y = eachLine[1]
    z = eachLine[2]
    print(x,y,z) 


# weight = [68, 81, 64, 56, 78, 74, 61, 77, 66, 68, 59, 71,
          # 80, 59, 67, 81, 69, 73, 69, 74, 70, 65]
          #
#plt.hist(rows, label='bins=10')
plt.hist(x,y,z label='bins=LG')
plt.legend()
plt.show()


